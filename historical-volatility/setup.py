from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Graph queries, notebooks, and scripts pertaining to the analysis of historical yield volatility.',
    author='vintro',
    license='',
)
